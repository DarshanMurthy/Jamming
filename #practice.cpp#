#include<iostream>
using namespace std;

struct Node{
	int add;
	struct Node * llink;
	struct Node * rrlink;
};

struct NodeTest{
  int add;
  struct Node * llink;
  struct Node * rrlink;
  
};



int main(){
        int test[10][10];
	int array[10][10]={{10,24,12},{10,29,34}};
	struct NodeTest * llink = malloc(sizeof(NodeTest));
	
	
	return 0;
}
